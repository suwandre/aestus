# apps/api

TypeScript/Bun HTTP API server. Single entry point for the frontend.

Owned by: API agents (P14–P15).
Depends on: `packages/contracts`, `packages/config`.
Connects to: Postgres, Redis, ClickHouse, NATS JetStream.
