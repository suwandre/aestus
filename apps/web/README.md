# apps/web

Frontend application (Next.js / React, TypeScript strict).

Owned by: frontend agents (P16–P24).
Depends on: `packages/contracts`, `packages/ui`, `packages/config`.
API: connects to `apps/api` over HTTP + WebSocket/SSE.
