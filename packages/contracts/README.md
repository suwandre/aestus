# packages/contracts

Shared TypeScript types, Zod schemas, and JSON Schema exports for every
data boundary in the system: asset identities, market events, feature
snapshots, anomalies, context packets, briefings, decisions, journal entries.

Owned by: contract agents (P03).
Imported by: all TypeScript services and the frontend.
Rust event model mirrors these in `crates/event_model`.
