# packages/config

Shared configuration schemas, defaults, and environment variable helpers
used across TypeScript services and the frontend.

Owned by: infrastructure/foundation agents (P01–P02).
Provides: typed env parsing, shared constants, feature flags (compile-time only).
