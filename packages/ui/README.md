# packages/ui

Shared React component library and design tokens for the Aestus cockpit UI.

Owned by: frontend agents (P16).
Tokens are derived verbatim from `docs/specs/reference/cockpit.html` `:root`.
IBM Plex Sans (UI) + IBM Plex Mono (tabular) fonts pinned.
Do not hardcode colors outside the token system.
