# Redis config — local dev
# Loaded via docker-compose command override

# Append-only file for BullMQ durability
appendonly yes
appendfsync everysec

# No password for local dev; add requirepass in production
# requirepass <password>

# Max memory policy (evict oldest keys when memory fills)
maxmemory-policy allkeys-lru
