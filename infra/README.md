# infra

Docker Compose files, NATS config, database init scripts, and
infrastructure-as-code for the local and production single-VPS deployment.

Owned by: infra agents (P02).
Local dev: `docker compose up` starts Postgres, Redis, ClickHouse, NATS JetStream.
See `docs/local_dev.md` for the full boot sequence.
