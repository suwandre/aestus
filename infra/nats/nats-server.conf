# NATS server config — local dev
# Monitoring endpoint at http://localhost:8222

port: 4222
http_port: 8222

jetstream {
  store_dir: /data/jetstream
  max_memory_store: 256MB
  max_file_store: 1GB
}
